import { LicenceCommerciale } from './licence-commerciale';

describe('LicenceCommerciale', () => {
  it('should create an instance', () => {
    expect(new LicenceCommerciale()).toBeTruthy();
  });
});
